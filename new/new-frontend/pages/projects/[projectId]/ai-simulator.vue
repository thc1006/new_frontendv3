<template>
  <div class="ai-simulator-page">
    <div class="page-header">
      <h1 class="page-title">AI Application Simulator</h1>
      <!-- 警告橫幅 -->
      <div class="warning-banner">
        <v-icon color="warning" size="20">mdi-alert-circle</v-icon>
        <span>Note: All data is generated by the simulator.</span>
      </div>
    </div>

    <div class="page-content">
      <!-- 左側 Model List 面板 -->
      <div class="model-list-panel">
        <div class="panel-title">Model list</div>
        <div class="model-buttons">
          <v-btn
            v-for="model in modelList"
            :key="model.id"
            :color="selectedModel === model.id ? 'success' : 'default'"
            :variant="selectedModel === model.id ? 'elevated' : 'tonal'"
            class="model-btn"
            @click="selectModel(model.id)"
          >
            {{ model.name }}
          </v-btn>
        </div>
      </div>

      <!-- 右側 Model Inference 面板 -->
      <div class="inference-panel">
        <div class="panel-title">Scene</div>

        <!-- 場景選擇器 -->
        <v-select
          v-model="selectedScene"
          :items="sceneOptions"
          label="Select Scene"
          density="compact"
          class="scene-select"
          hide-details
        />

        <!-- 地圖/視覺化區域 placeholder -->
        <div class="map-placeholder">
          <v-icon size="64" color="grey">mdi-map</v-icon>
          <p>視覺化區域</p>
          <p class="text-caption text-grey">TODO: 整合 3D 地圖與 Heatmap</p>
        </div>

        <!-- Heatmap 控制 -->
        <div class="heatmap-control">
          <v-switch
            v-model="showHeatmap"
            label="Heatmap"
            density="compact"
            hide-details
          />
          <v-select
            v-model="heatmapType"
            :items="['RSRP']"
            density="compact"
            class="heatmap-type-select"
            hide-details
          />
        </div>
      </div>
    </div>

    <!-- 提示訊息 -->
    <v-snackbar v-model="snackbar.show" :color="snackbar.color" timeout="3000" location="top">
      {{ snackbar.text }}
    </v-snackbar>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'

// Model 列表
const modelList = [
  { id: 'nes', name: 'NES' },
  { id: 'positioning', name: 'Positioning' },
  { id: 'im', name: 'IM' },
  { id: 'mro', name: 'MRO' },
  { id: 'rs', name: 'RS' },
  { id: 'bc', name: 'BC' }
]

const selectedModel = ref<string | null>(null)

// 場景選項 (根據 Figma 277:592)
const sceneOptions = [
  'None',
  '上班',
  '下班',
  '上課1/隨機',
  '上課2/同步'
]
const selectedScene = ref('None')

// Heatmap 控制
const showHeatmap = ref(false)
const heatmapType = ref('RSRP')

const snackbar = ref({ show: false, text: '', color: 'info' })

function selectModel(modelId: string) {
  selectedModel.value = modelId
  snackbar.value = {
    show: true,
    text: `已選擇 ${modelId.toUpperCase()} 模型（功能開發中）`,
    color: 'info'
  }
  console.warn(`[TODO] ${modelId} model page not implemented`)
}
</script>

<style scoped>
.ai-simulator-page {
  padding: 24px;
  max-width: 1400px;
  margin: 0 auto;
}

.page-header {
  display: flex;
  align-items: center;
  gap: 24px;
  margin-bottom: 24px;
  flex-wrap: wrap;
}

.page-title {
  font-size: 24px;
  font-weight: 500;
  color: #333;
  margin: 0;
}

.warning-banner {
  display: flex;
  align-items: center;
  gap: 8px;
  background: #fff3e0;
  padding: 8px 16px;
  border-radius: 20px;
  font-size: 14px;
  color: #e65100;
}

.page-content {
  display: flex;
  gap: 24px;
}

.model-list-panel {
  width: 180px;
  flex-shrink: 0;
  background: #fff;
  border-radius: 8px;
  padding: 16px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.panel-title {
  font-size: 18px;
  font-weight: 500;
  margin-bottom: 16px;
  color: #333;
}

.model-buttons {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.model-btn {
  text-transform: none;
  justify-content: center;
}

.inference-panel {
  flex: 1;
  background: #fff;
  border-radius: 8px;
  padding: 16px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.scene-select {
  max-width: 200px;
  margin-bottom: 16px;
}

.map-placeholder {
  background: #f5f5f5;
  border: 2px dashed #ccc;
  border-radius: 8px;
  min-height: 400px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  margin-bottom: 16px;
}

.heatmap-control {
  display: flex;
  align-items: center;
  gap: 16px;
  padding: 12px;
  background: #f9f9f9;
  border-radius: 8px;
}

.heatmap-type-select {
  max-width: 120px;
}

@media (max-width: 768px) {
  .page-content {
    flex-direction: column;
  }

  .model-list-panel {
    width: 100%;
  }

  .model-buttons {
    flex-direction: row;
    flex-wrap: wrap;
  }
}
</style>
